apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  labels:
    app: demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ciao
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: demo2
          image: louisadjams/demo4:latest
